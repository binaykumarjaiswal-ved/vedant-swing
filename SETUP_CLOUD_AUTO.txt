VEDANT SWING — AUTOMATIC CLOUD SETUP
====================================

DONE FOR YOU:
  GitHub repo: https://github.com/binaykumarjaiswal-ved/vedant-swing
  Mode: morning research only (evening scan removed)

OPTION A — Google Cloud Shell
  1. Open https://console.cloud.google.com → Cloud Shell
  2. Run: bash gcp/bootstrap.sh
  3. Edit code — push via git

OPTION B — Free web app on Render
  1. Open https://dashboard.render.com → New → Blueprint
  2. Connect repo: binaykumarjaiswal-ved/vedant-swing
  3. Render reads render.yaml (free tier)

SECRETS (dashboards only — never share in chat):
  TELEGRAM_BOT_TOKEN  → optional alerts
  GROQ_API_KEY        → AI morning briefing
  GITHUB_TOKEN        → Render sync if private state

DATA: Yahoo Finance via yfinance (NSE .NS symbols)

BROKER: Not connected. Recommend-only — you trade manually.
